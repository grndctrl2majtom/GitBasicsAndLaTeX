# Ignore everything by default
*

# But allow directories to be traversed so .tex/.pdf files
# inside subfolders aren't skipped
!*/

# Un-ignore .tex and .pdf files, at any depth
!*.tex
!*.png
!main.pdf

# Un-ignore this file itself so it stays tracked
!.gitignore